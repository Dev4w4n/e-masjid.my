name: "CodeQL Configuration"

paths-ignore:
  - "legacy/**"
  - "**/node_modules"
  - "**/dist"
  - "**/build"
  - "test-results/**"
# Optional: You can also specify paths to include if needed
# paths:
#   - "apps/**"
#   - "packages/**"

# Optional: Disable default queries and only run security queries
# disable-default-queries: true

# Optional: Specify custom query suites
# queries:
#   - uses: security-extended
#   - uses: security-and-quality
