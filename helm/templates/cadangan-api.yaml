apiVersion: v1
kind: Service
metadata:
  name: cadangan-api
  namespace: "{{ .Values.global.env }}-{{ .Values.global.tenant }}"
  labels:
    app: cadangan-api
    tenant: {{ .Values.global.tenant }}
    env: {{ .Values.global.env | quote }}
spec:
  selector:
    app: cadangan-api
    tenant: {{ .Values.global.tenant }}
    env: {{ .Values.global.env | quote }}
  type: NodePort
  ports:
    - protocol: TCP
      port: {{ .Values.services.cadanganApi.port }}
      targetPort: {{ .Values.services.cadanganApi.port }}
      nodePort: {{ .Values.services.cadanganApi.nodePort }}
      
---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: cadangan-api
  namespace: "{{ .Values.global.env }}-{{ .Values.global.tenant }}"
  labels:
    app: cadangan-api
    tenant: {{ .Values.global.tenant }}
    env: {{ .Values.global.env | quote }}
spec:
  selector:
    matchLabels:
      app: cadangan-api
      tenant: {{ .Values.global.tenant }}
      env: {{ .Values.global.env | quote }}
  replicas: 1
  template:
    metadata:
      labels:
        app: cadangan-api
        tenant: {{ .Values.global.tenant }}
        env: {{ .Values.global.env | quote }}
    spec:
      containers:
        - name: cadangan-api
          image: {{ .Values.services.cadanganApi.image }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          env:
            {{- range .Values.services.cadanganApi.env }}
            - name: {{ .name | quote }}
              value: {{ .value | quote }}
            {{- end }}
            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: "{{ .Values.global.env }}-{{ .Values.global.tenant }}-db-config"
                  key: DB_HOST
            - name: DB_PORT
              valueFrom:
                configMapKeyRef:
                  name: "{{ .Values.global.env }}-{{ .Values.global.tenant }}-db-config"
                  key: DB_PORT
            - name: DB_NAME
              valueFrom:
                configMapKeyRef:
                  name: "{{ .Values.global.env }}-{{ .Values.global.tenant }}-db-config"
                  key: DB_NAME
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: "{{ .Values.global.env }}-{{ .Values.global.tenant }}-db-secret"
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: "{{ .Values.global.env }}-{{ .Values.global.tenant }}-db-secret"
                  key: DB_PASSWORD
          ports:
            - containerPort: {{ .Values.services.cadanganApi.port }}
          args: ["./wait-for-it.sh", "saas-api:8080", "--", "./cadangan-api"]
