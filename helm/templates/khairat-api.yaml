apiVersion: v1
kind: Service
metadata:
  name: khairat-api
  labels:
    app: khairat-api
    tenant: {{ .Values.global.tenant }}
spec:
  selector:
    app: khairat-api
    tenant: {{ .Values.global.tenant }}
  type: NodePort
  ports:
    - protocol: TCP
      port: {{ .Values.services.khairatApi.port }}
      targetPort: {{ .Values.services.khairatApi.port }}
      nodePort: {{ .Values.services.khairatApi.nodePort }}
      
---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: khairat-api
  labels:
    app: khairat-api
    tenant: {{ .Values.global.tenant }}
spec:
  selector:
    matchLabels:
      app: khairat-api
      tenant: {{ .Values.global.tenant }}
  replicas: 1
  template:
    metadata:
      labels:
        app: khairat-api
        tenant: {{ .Values.global.tenant }}
    spec:
      containers:
        - name: khairat-api
          image: {{ .Values.services.khairatApi.image }}
          imagePullPolicy: IfNotPresent
          resources:
            limits:
              memory: "256Mi"
              cpu: "250m"
            requests:
              memory: "128Mi"
              cpu: "125m" 
          env:
            - name: EMASJID_REACT_APP_BUILD_VERSION
              value: "v2.3.1"
            - name: EMASJID_REACT_APP_DOMAIN
              value: "e-masjid.my"
            - name: GO_ENV
              value: "local"
            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: db-config
                  key: DB_HOST
            - name: DB_PORT
              valueFrom:
                configMapKeyRef:
                  name: db-config
                  key: DB_PORT
            - name: DB_NAME
              valueFrom:
                configMapKeyRef:
                  name: db-config
                  key: DB_NAME
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: db-secret
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-secret
                  key: DB_PASSWORD
            - name: ALLOWED_ORIGIN
              value: "*"
            - name: DEPLOY_URL
              value: "/"
            - name: SERVER_PORT
              value: "8081"
            - name: API_DOC_URL
              value: http://localhost:4000"
          ports:
            - containerPort: {{ .Values.services.khairatApi.port }}
          args: ["./wait-for-it.sh", "saas-api:8080", "--", "./khairat-api"]

