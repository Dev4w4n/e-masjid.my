{
  "name": "@masjid-suite/supabase-client",
  "version": "1.0.0",
  "private": true,
  "description": "Supabase client configuration for Masjid Suite",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit",
    "test": "vitest",
    "test:watch": "vitest --watch",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint src --ext .ts",
    "lint:fix": "eslint src --ext .ts --fix"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "@masjid-suite/shared-types": "workspace:*"
  },
  "devDependencies": {
    "@masjid-suite/eslint-config": "workspace:*",
    "typescript": "^5.2.0",
    "vitest": "^1.0.0",
    "eslint": "^8.57.0"
  }
}
